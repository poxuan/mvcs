<?php

namespace $model_ns;

$model_bu

class $model_name $model_bn
{
    // 表名
    protected $table = '$table_name';

    // 可填充字段
    protected $fillable = [
        $fillable_column
    ];
    // 不展示字段
    protected $hidden = [
        'org_id','deleted_at','deleted_by'
    ];

    //栏位验证信息
    public static $message = [
        //todo 验证信息
    ];

    //todo 建立依赖关系
    $model_relay
}
