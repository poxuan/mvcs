/**
     * 启用/停用验证规则
     *
     * @author $author_info
     * @since  $sub_version
     * @param  int $id 
     * @return void
     */
    public static function upDown($id)
    {
        $models = $model_name::findOrFail($id);
        {user:author}
        if ($model->author_id != user()->id) {
            throw new \Exception('',40300);
        }
        {user}
        if ($model->user_id != user()->id) {
            throw new \Exception('',40300);
        }
        {/user}
    }
