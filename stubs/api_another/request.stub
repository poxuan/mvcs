<?php

namespace $request_ns;

$request_use

class $request_name $request_extends
{
    // 公共部分
    public $rules=[
    ];

    //这里我只写了部分字段，可以定义全部字段
    protected $messages=[
        $validator_message
    ];

    public function store(){  //返回自定义消息，不使用的话为默认提示
        $this->rules = [
            $validator_rule
        ];
    }
}
